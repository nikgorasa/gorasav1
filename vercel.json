{
  "version": 2,
  "buildCommand": "cd packages/backend && npx prisma generate && cd ../.. && npm run build -w packages/frontend",
  "outputDirectory": "packages/frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
